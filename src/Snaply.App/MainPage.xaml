<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Snaply.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:ctrl="using:Snaply.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="720"
    d:DesignWidth="1100"
    HighContrastAdjustment="None"
    mc:Ignorable="d">

    <Grid>
        <!--  Preview card with floating command bars.  -->
        <Border
            Margin="{StaticResource ContentPadding}"
            Style="{StaticResource PreviewSurfaceStyle}">
            <Grid>
                <Grid.Resources>
                    <!--  Saved feedback: the folder icon flips to a green check and back.
                          Played by the code-behind after each successful auto-save.  -->
                    <Storyboard x:Name="SavedFeedback">
                        <DoubleAnimationUsingKeyFrames Storyboard.TargetName="FolderGlyph" Storyboard.TargetProperty="Opacity">
                            <LinearDoubleKeyFrame KeyTime="0:0:0" Value="1" />
                            <LinearDoubleKeyFrame KeyTime="0:0:0.1" Value="0" />
                            <LinearDoubleKeyFrame KeyTime="0:0:0.9" Value="0" />
                            <LinearDoubleKeyFrame KeyTime="0:0:1.05" Value="1" />
                        </DoubleAnimationUsingKeyFrames>
                        <DoubleAnimationUsingKeyFrames Storyboard.TargetName="SavedCheck" Storyboard.TargetProperty="Opacity">
                            <LinearDoubleKeyFrame KeyTime="0:0:0" Value="0" />
                            <LinearDoubleKeyFrame KeyTime="0:0:0.1" Value="1" />
                            <LinearDoubleKeyFrame KeyTime="0:0:0.9" Value="1" />
                            <LinearDoubleKeyFrame KeyTime="0:0:1.05" Value="0" />
                        </DoubleAnimationUsingKeyFrames>
                        <DoubleAnimationUsingKeyFrames Storyboard.TargetName="SavedCheckScale" Storyboard.TargetProperty="ScaleX">
                            <LinearDoubleKeyFrame KeyTime="0:0:0" Value="0.6" />
                            <SplineDoubleKeyFrame KeyTime="0:0:0.28" Value="1.2" KeySpline="0.1,0.9,0.2,1" />
                            <SplineDoubleKeyFrame KeyTime="0:0:0.44" Value="1" KeySpline="0.2,0,0,1" />
                        </DoubleAnimationUsingKeyFrames>
                        <DoubleAnimationUsingKeyFrames Storyboard.TargetName="SavedCheckScale" Storyboard.TargetProperty="ScaleY">
                            <LinearDoubleKeyFrame KeyTime="0:0:0" Value="0.6" />
                            <SplineDoubleKeyFrame KeyTime="0:0:0.28" Value="1.2" KeySpline="0.1,0.9,0.2,1" />
                            <SplineDoubleKeyFrame KeyTime="0:0:0.44" Value="1" KeySpline="0.2,0,0,1" />
                        </DoubleAnimationUsingKeyFrames>
                    </Storyboard>
                </Grid.Resources>

                <!--  Ambient backdrop: always-on animated background. Muted (lower saturation
                      and drift) while an image is shown.  -->
                <ctrl:AmbientBackdrop
                    x:Name="AmbientBackdrop"
                    IsActive="True"
                    IsMuted="{x:Bind ViewModel.HasImage, Mode=OneWay}" />

                <!--  The image fills the whole card so zoom/pan crops at the card edge, not an inset.  -->
                <ctrl:ZoomableImage
                    x:Name="PreviewImageControl"
                    Source="{x:Bind ViewModel.Preview, Mode=OneWay}" />

                <!--  Floating UI (command bars, error banner, progress), inset from the card edge.  -->
                <Grid Padding="{StaticResource PreviewSurfacePadding}">

                    <!--  Capture, top-left. The menu picks the mode; the button body captures.  -->
                    <Border
                        x:Name="CommandBar"
                        Margin="{StaticResource FloatingToolbarMargin}"
                        HorizontalAlignment="Left"
                        VerticalAlignment="Top"
                        Style="{StaticResource FloatingToolbarStyle}">
                        <SplitButton
                            x:Name="CaptureButton"
                            AutomationProperties.AutomationId="CaptureButton"
                            Command="{x:Bind ViewModel.CaptureCommand}">
                            <StackPanel Orientation="Horizontal" Spacing="{StaticResource SpacingSmall}">
                                <FontIcon
                                    x:Name="PrimaryCaptureGlyph"
                                    FontSize="{StaticResource IconSizeSmall}"
                                    Glyph="&#xE7F4;" />
                                <TextBlock x:Name="PrimaryCaptureLabel" />
                            </StackPanel>
                            <SplitButton.Flyout>
                                <MenuFlyout Placement="Bottom">
                                    <MenuFlyoutItem
                                        x:Name="RegionCaptureItem"
                                        x:Uid="RegionCaptureItem"
                                        AutomationProperties.AutomationId="RegionCaptureItem"
                                        Click="RegionCaptureItem_Click">
                                        <MenuFlyoutItem.Icon>
                                            <FontIcon Glyph="&#xEF20;" />
                                        </MenuFlyoutItem.Icon>
                                    </MenuFlyoutItem>
                                    <MenuFlyoutItem
                                        x:Name="WindowCaptureItem"
                                        x:Uid="WindowCaptureItem"
                                        AutomationProperties.AutomationId="WindowCaptureItem"
                                        Click="WindowCaptureItem_Click">
                                        <MenuFlyoutItem.Icon>
                                            <FontIcon Glyph="&#xE737;" />
                                        </MenuFlyoutItem.Icon>
                                    </MenuFlyoutItem>
                                    <MenuFlyoutItem
                                        x:Name="DesktopCaptureItem"
                                        x:Uid="DesktopCaptureItem"
                                        AutomationProperties.AutomationId="DesktopCaptureItem"
                                        Click="DesktopCaptureItem_Click">
                                        <MenuFlyoutItem.Icon>
                                            <FontIcon Glyph="&#xE7F4;" />
                                        </MenuFlyoutItem.Icon>
                                    </MenuFlyoutItem>
                                </MenuFlyout>
                            </SplitButton.Flyout>
                        </SplitButton>
                    </Border>

                    <!--  Open Folder, top-right. Opens the captures folder; its icon flips to a
                          green check on each auto-save (see the SavedFeedback storyboard above).  -->
                    <Border
                        x:Name="OpenFolderBar"
                        Margin="{StaticResource FloatingToolbarMargin}"
                        HorizontalAlignment="Right"
                        VerticalAlignment="Top"
                        Style="{StaticResource FloatingToolbarStyle}">
                        <Button
                            x:Name="OpenFolderButton"
                            Padding="{StaticResource IconButtonPadding}"
                            AutomationProperties.AutomationId="OpenFolderButton"
                            Background="Transparent"
                            BorderThickness="0"
                            Click="OpenFolderButton_Click">
                            <Grid>
                                <FontIcon
                                    x:Name="FolderGlyph"
                                    FontSize="{StaticResource IconSizeSmall}"
                                    Glyph="&#xE8B7;" />
                                <FontIcon
                                    x:Name="SavedCheck"
                                    FontSize="{StaticResource IconSizeSmall}"
                                    Foreground="{ThemeResource SystemFillColorSuccessBrush}"
                                    Glyph="&#xE73E;"
                                    Opacity="0"
                                    RenderTransformOrigin="0.5,0.5">
                                    <FontIcon.RenderTransform>
                                        <ScaleTransform x:Name="SavedCheckScale" ScaleX="0.6" ScaleY="0.6" />
                                    </FontIcon.RenderTransform>
                                </FontIcon>
                            </Grid>
                        </Button>
                    </Border>

                    <!--  Error banner (bottom), dismissible.  -->
                    <InfoBar
                        x:Name="ErrorInfoBar"
                        Margin="{StaticResource ContentPadding}"
                        VerticalAlignment="Bottom"
                        AutomationProperties.AutomationId="ErrorInfoBar"
                        IsClosable="True"
                        IsOpen="{x:Bind ViewModel.HasError, Mode=TwoWay}"
                        Message="{x:Bind ViewModel.ErrorMessage, Mode=OneWay}"
                        Severity="Error" />

                    <ProgressRing
                        Width="{StaticResource ProgressRingSize}"
                        Height="{StaticResource ProgressRingSize}"
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center"
                        IsActive="{x:Bind ViewModel.CaptureCommand.IsRunning, Mode=OneWay}" />
                </Grid>
            </Grid>
        </Border>
    </Grid>
</Page>
